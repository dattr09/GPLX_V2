@model IEnumerable<GPLX.Models.KetQuaThiGplx>

@{
    ViewData["Title"] = "Danh sách kết quả thi GPLX";
}

<h1 class="text-center text-primary">@ViewData["Title"]</h1>
<hr />

<div class="d-flex justify-content-between mb-3">
    <a href="@Url.Action("Create")" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Tạo mới kết quả thi GPLX
    </a>
    <form class="d-flex">
        <input type="text" id="searchInput" class="form-control me-2" placeholder="🔍 Tìm kiếm theo Mã...">
    </form>
</div>

<table class="table table-hover table-bordered shadow-sm text-center align-middle" id="ketQuaTable">
    <thead class="table-dark">
        <tr>
            <th>Mã KQ</th>
            <th>Lý Thuyết</th>
            <th>Thực Hành</th>
            <th>Mô Phỏng</th>
            <th>Đường Trường</th>
            <th>Kết Quả</th>
            <th>Ghi Chú</th>
            <th>Hành Động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td><strong>@item.MaKetQua</strong></td>
                <td>@item.DiemLyThuyet</td>
                <td>@item.DiemThucHanh</td>
                <td>@(item.DiemMoPhong.HasValue ? item.DiemMoPhong.Value.ToString() : "N/A")</td>
                <td>@(item.DiemDuongTruong.HasValue ? item.DiemDuongTruong.Value.ToString() : "N/A")</td>
                <td>
                    <span class="badge @(item.KetQua == "Đậu" ? "bg-success" : "bg-danger")">
                        @item.KetQua
                    </span>
                </td>
                <td>@(string.IsNullOrEmpty(item.GhiChu) ? "Không có" : item.GhiChu)</td>
                <td>
                    <a href="@Url.Action("Details", new { id = item.MaKetQua })" class="btn btn-info btn-sm" title="Xem">
                        <i class="bi bi-eye"></i>Xem
                    </a>
                    <a href="@Url.Action("Edit", new { id = item.MaKetQua })" class="btn btn-warning btn-sm" title="Chỉnh sửa">
                        <i class="bi bi-pencil"></i>Sửa
                    </a>
                    <a href="@Url.Action("Delete", new { id = item.MaKetQua })" class="btn btn-danger btn-sm" title="Xóa">
                        <i class="bi bi-trash"></i>Xoá
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const searchInput = document.getElementById("searchInput");
        searchInput.addEventListener("keyup", function () {
            const filter = searchInput.value.toLowerCase();
            const rows = document.querySelectorAll("#ketQuaTable tbody tr");

            rows.forEach(row => {
                const maKetQua = row.cells[0].textContent.toLowerCase();
                const ketQua = row.cells[5].textContent.toLowerCase();
                row.style.display = (maKetQua.includes(filter) || ketQua.includes(filter)) ? "" : "none";
            });
        });
    });
</script>
